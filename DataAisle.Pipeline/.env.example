# DataAisle — environment variables
# Copy this to .env and adjust if your SQL Server instance name differs.
# .env is in .gitignore — never commit it.

DA_SQL_SERVER=localhost\SQLEXPRESS
DA_SQL_DATABASE=DataAisle
